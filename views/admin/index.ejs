<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin – GolfLigan</title>
  <style>
    body { font-family: Arial; margin: 2rem; background: #f9f9f9; }
    .container { max-width: 900px; margin: 0 auto; background: white; padding: 2rem; border-radius: 10px; }
    table { width: 100%; border-collapse: collapse; margin-top: 1rem; }
    th, td { padding: 0.8rem; border: 1px solid #ddd; text-align: left; }
    th { background: #1a5d1a; color: white; }
    .btn { padding: 0.5rem 1rem; background: #1a5d1a; color: white; text-decoration: none; border-radius: 5px; }
    .btn:hover { background: #146b14; }
    .btn-small { padding: 0.3rem 0.6rem; font-size: 0.8rem; }
    .logout { background: #aaa; float: right; }
  </style>
</head>
<body>
  <div class="container">
    <h1>Admin: <%= playerName %></h1>
    <a href="/" class="btn logout">Till ligan</a>
    <a href="/admin/game-day/new" class="btn">+ Ny speldag</a>
    <hr>

    <h2>Speldagar</h2>
    <% if (gameDays.length === 0) { %>
      <p><em>Inga speldagar än. Skapa en!</em></p>
    <% } else { %>
      <table>
        <tr>
          <th>Datum</th>
          <th>Tee</th>
          <th>Spelform</th>
          <th>Sidospel</th>
          <th>Beergame</th>
          <th>Åtgärd</th>
        </tr>
        <% gameDays.forEach(day => { %>
          <tr>
            <td><%= day.date %></td>
            <td><%= day.tee %></td>
            <td><%= day.gameMode %></td>
            <td><%= day.sideGame || '—' %></td>
            <td><%= day.beerGameEnabled ? 'Ja' : 'Nej' %></td>
            <td>
              <a href="/game-day/<%= day.id %>" class="btn btn-small">Visa</a>
            </td>
          </tr>
        <% }) %>
      </table>
    <% } %>
  </div>
</body>
</html>