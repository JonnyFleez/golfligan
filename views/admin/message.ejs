<!DOCTYPE html>
<html lang="sv">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redigera meddelande – Admin</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            padding: 2rem;
            margin: 0;
        }

        .card {
            max-width: 700px;
            margin: 0 auto;
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            color: #1a5d1a;
            text-align: center;
            margin-bottom: 1.5rem;
        }

        textarea {
            width: 100%;
            height: 120px;
            padding: 1rem;
            font-size: 1rem;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin: 1rem 0;
            box-sizing: border-box;
            resize: vertical;
        }

        .button-group {
            display: flex;
            gap: 1rem;
            margin-top: 1rem;
            justify-content: flex-start;
        }

        button {
            background: #1a5d1a;
            color: white;
            padding: 0.8rem 1.5rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: bold;
            transition: background 0.2s;
        }

        button:hover {
            background: #146b14;
        }

        .btn-primary {
            background: #1a5d1a;
            color: white;
            padding: 0.8rem 1.5rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
        }

        .btn-primary:hover {
            background: #146b14;
        }

        .btn-back:hover {
            background: #1f5b1f;
        }
    </style>
</head>

<body>
    <div class="card">
        <h2>Redigera meddelande till alla spelare</h2>

        <form method="POST" action="/admin/message">
            <textarea name="message" placeholder="Skriv ditt meddelande här..." required><%= message %></textarea>

            <div class="button-group">
                <button type="submit">Spara meddelande</button>
                <button type="button" onclick="window.location.href='/'" class="btn-primary">Huvudmeny</button>
            </div>
        </form>
    </div>
</body>

</html>